

env = Environment(
  CCFLAGS = [
    '-Wall', '-g3', '-std=c++11',
    #'-ffast-math', '-funroll-loops', '-pipe', '-Wno-deprecated', '-msse2', '-msse3',
    #'-mfpmath=sse', '-fomit-frame-pointer'
  ],

  CPPPATH=['./ml','./math', './test'],
  LIBS=[],
  LIBPATH=[],
  CPPDEFINES=[],
  LINKFLAGS=['-s', '-Os']
)


env.Program( './test/example', './test/example.cpp' )
env.Program( './test/benchmark', './test/benchmark.cpp' )
