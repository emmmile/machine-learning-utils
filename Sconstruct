

env = Environment( 
  CCFLAGS = [
    '-Wall', '-O3', '-ffast-math', '-funroll-loops', 
    '-std=c++0x', '-pipe', '-Wno-deprecated', '-msse2', '-mfpmath=sse', '-fomit-frame-pointer'
  ],

  CPPPATH=['./ml','./ml/random', './test'],
  LIBS=[],
  LIBPATH=[],
  CPPDEFINES=[],
  LINKFLAGS=['-s', '-Os']
)


env.Program( './test/test', './test/main.cpp' )
env.Program( './test/benchmark', './test/benchmark.cpp' )
